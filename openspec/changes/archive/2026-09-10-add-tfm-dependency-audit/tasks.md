## 1. Каркас проекта

- [x] 1.1 Создать `src/TfmAudit/TfmAudit.csproj` под `net6.0`, `LangVersion 10.0`, `Nullable`,
      `TreatWarningsAsErrors`, `RollForward=LatestMajor`, без `PackageReference`.
      Проверка: `dotnet build -c Release` проходит с нулём предупреждений
- [x] 1.2 Добавить `TfmAudit.sln` и `.gitignore`. Проверка: решение открывается в Rider/VS

## 2. Модель и чтение assets

- [x] 2.1 Реализовать `Model/Tfm.cs`: разбор `netX.Y`, `netcoreappX.Y`, `netstandardX.Y`, `netNN`,
      платформенных суффиксов и алиасов с RID; семейства и сравнение версий внутри семейства.
      Проверка: `net4.8` попадает в .NET Framework, `net8.0` и `netcoreapp3.1` — в одно семейство,
      неизвестный монитор не выбрасывает исключение
- [x] 2.2 Реализовать модель `project.assets.json` и её чтение через `JsonDocument`: `targets`,
      `libraries`, `projectFileDependencyGroups`, `project.frameworks`, `project.restore.frameworks`.
      Проверка: разбор реального примера на 231 узел, отсутствие падений на необязательных полях
- [x] 2.3 Прочитать `projectReferences` из `project.restore.frameworks`.
      Проверка: в примере находятся четыре прямых ProjectReference на каждый таргет
- [x] 2.4 Реализовать `Analysis/AssetPathParser.cs`: извлечение TFM из `lib/`, `ref/`, `embed/`,
      `runtimes/<rid>/lib/`, `build*/`, `contentFiles/`, `tools/`; распознавание заглушки `_._`.
      Проверка: `analyzers/dotnet/cs/*.dll` даёт «без TFM», `lib/net6.0/_._` — заглушку

## 3. Анализ и атрибуция

- [x] 3.1 Классифицировать пакеты по разрыву поколений; уровни: ≥2 — ошибка, 1 — предупреждение,
      `netstandard2.x` — информация, `netstandard1.x` и `frameworkAssemblies` — предупреждение,
      чужое семейство — ошибка. Проверка: на примере под `net8.0` — 51/17/60, под `net6.0` — 10/26/68
- [x] 3.2 Понижать уровень на ступень, если устарели только MSBuild-ассеты.
      Проверка: пакет с `compile: lib/net8.0` и `build/net6.0` не даёт ошибки
- [x] 3.3 Построить граф на нормализованных идентификаторах; корни — `PackageReference`,
      `ProjectReference` и узлы с нулевой входящей степенью.
      Проверка: ни у одной находки в примере глубина не равна нулю; в цепочках
      `Microsoft.Data.SqlClient`, а не `microsoft.data.sqlclient`
- [x] 3.4 Считать глубины, достижимость от каждого корня и кратчайшие цепочки; ограничивать число
      показываемых цепочек. Проверка: `Microsoft.Identity.Client` показан на глубине 4 с двумя
      виновниками и корректными цепочками
- [x] 3.5 Заменить жадное покрытие честным расчётом влияния: эксклюзивные находки, счётчики
      «достижимо из одного корня» и «из нескольких», сортировка по влиянию.
      Проверка: сумма двух счётчиков равна общему числу находок таргета
- [x] 3.6 Добавить базу знаний о пакетах, входящих в состав рантайма, и о контрактах netstandard 1.x.
      Проверка: `System.Text.Json 4.7.2` под `net8.0` получает подсказку, формулировка — гипотеза
- [x] 3.7 Реализовать фильтры: порог уровня, игнорирование по имени и префиксу, отбор таргетов.
      Проверка: `--min-severity error` оставляет только ошибки, `--ignore Contoso.Platform.*` убирает внутренние

## 4. Режим опроса NuGet-фида

- [x] 4.1 Читать `NuGet.config` от папки с assets до корня диска плюс пользовательский конфиг;
      учитывать `<clear/>`, `disabledPackageSources`, `packageSourceCredentials`.
      Проверка: отключённый источник не опрашивается, приоритет у конфига ближе к проекту
- [x] 4.2 Реализовать клиент NuGet V3 на `HttpClient`: service index → `PackageBaseAddress/3.0.0`,
      список версий, `.nuspec`, чтение папок из `.nupkg` при `--deep`, Basic-авторизация, ретраи.
      Проверка: недоступный источник помечается причиной, анализ продолжается
- [x] 4.3 Реализовать семантику версий NuGet (четыре компонента, pre-release, build-метаданные).
      Проверка: релиз старше pre-release той же версии, числовые части pre-release сравниваются как числа
- [x] 4.4 Реализовать бинарный поиск минимальной версии с ассетом ровно под целевой TFM; отдельные
      формулировки для «пакет заброшен» и «пакет собирается только под .NET Standard».
      Проверка: папка `net9.0` при таргете `net8.0` не считается поддержкой
- [x] 4.5 Не опрашивать фид по `ProjectReference`. Проверка: локальные проекты не дают запросов
- [x] 4.6 Не падать при прерывании и недоступности всех источников — дописывать примечание в отчёт

## 5. Отчёты

- [x] 5.1 Консольный отчёт: сводка, гистограмма TFM, раздел «Что обновить», находки с цепочками;
      ANSI-цвета с отключением по `NO_COLOR`, флагу и при перенаправлении вывода
- [x] 5.2 Русские числительные во всех формулировках.
      Проверка: «6 мажорных поколений назад», «затрагивает 1 находку», «2 находки», «5 находок»
- [x] 5.3 Markdown-отчёт с таблицами и раскрывающимися блоками
- [x] 5.4 JSON-отчёт для CI со всеми находками, цепочками, рекомендациями и советами фида
- [x] 5.5 Graphviz DOT и DGML с раскраской по уровням и выделением путей до находок.
      Проверка: `dot -Tsvg` рендерит файл, DGML открывается в Visual Studio
- [x] 5.6 Самодостаточный HTML: шаблон в ресурсах сборки, данные как JSON с экранированием
      угловых скобок; вкладки по таргетам, карточки, гистограмма, фильтры по уровню, TFM и поиску
- [x] 5.7 Послойный интерактивный граф в HTML: слои по глубине, медианная эвристика порядка,
      панорама и масштаб, подсветка предков и потомков, панель узла, переключение объёма показа
      (только ошибки / все находки / весь граф). Проверка: 81 узел в режиме ошибок против 231 в полном

## 6. Командная строка

- [x] 6.1 Разбор аргументов и справка на русском с примерами и кодами возврата
- [x] 6.2 Разрешение входных путей: assets-файл, папка проекта, csproj/fsproj/vbproj, sln,
      папка решения с рекурсивным поиском и пропуском `bin`, `node_modules`, `.git`, `.vs`, `.idea`
- [x] 6.3 Коды возврата: 0 — успех, 1 — ошибка выполнения, 2 — порог `--fail-on`.
      Проверка: `--fail-on error` даёт 2, `--fail-on none` даёт 0, несуществующий путь даёт 1
- [x] 6.4 Отклонять `--deep` без `--online` до начала анализа

## 7. Проверка

- [x] 7.1 Сборка `-c Release` с нулём предупреждений при `TreatWarningsAsErrors`
- [x] 7.2 Прогон на `samples/project.assets.json` по обоим таргетам, сверка находок и гистограммы
      с ручным разбором JSON
- [x] 7.3 Рендер HTML в headless-браузере: нет ошибок в консоли, работают вкладки, фильтры,
      переключатели графа и панель узла
- [x] 7.4 Проверка HTML на ширинах 400 / 768 / 1440 — без горизонтальной прокрутки страницы —
      и в тёмной теме
- [x] 7.5 Документация: README с описанием уровней, форматов, режима `--online` и примерами
- [ ] 7.6 Собрать под `net6.0` на машине с таргетинг-паком `Microsoft.NETCore.App.Ref 6.0.x`.
      В окружении разработки хосты Microsoft и nuget.org закрыты политикой, поэтому компиляция
      проверялась под `net8.0` при `LangVersion 10.0`; API-поверхность ограничена доступным в net6,
      но фактическую net6-сборку надо подтвердить

## 8. Возможные следующие шаги

- [ ] 8.1 Автотесты на `Tfm`, `NuGetVersion` и классификацию ассетов на синтетических assets-файлах
- [ ] 8.2 Поддержка Central Package Management (`Directory.Packages.props`) в советах по обновлению
- [ ] 8.3 Режим сравнения двух прогонов: что изменилось после обновления пакетов
